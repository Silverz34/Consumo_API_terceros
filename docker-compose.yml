version : '3.8'
services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app_container
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
